valid triangle
input:60 60 60
output:valid

#include <stdio.h>
int main() {
    int a=60;
    int b=60;
    int c=60;
    printf("%d %d %d",a,b,c);
    if(a+b+c==180 &&a>0&&b>0&&c>0){
        printf("valid triangle");
    }
    else{
        printf("no valid triangle");
    }
    return 0;
}