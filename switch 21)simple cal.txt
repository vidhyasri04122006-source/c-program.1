simple cal (switch)
input:5+3
output:8

#include <stdio.h>

int main() {
    int a,b;
    char op:
    scanf("%d %d",&a,&b,&op);
    switch(sum){
        case'+':
        printf("%d",a+b);
        break;
        
        default:
        printf("invalid");
    }
    return 0;
}
    